---
import { ArrowUpRight } from "@lucide/astro";
import { Image } from "astro:assets";

const { p: item } = Astro.props;
---

<div
  class="min-w-[280px] w-[85vw] md:w-[400px] shrink-0 snap-center group relative h-[450px] md:h-[550px] rounded-[2.5rem] overflow-hidden bg-slate-200 shadow-lg transition-all duration-700"
>
  <Image
    src={item.image}
    alt={item.title}
    width={400}
    height={600}
    loading="lazy"
    class="absolute inset-0 w-full h-full object-cover transition-transform duration-1000 md:group-hover:scale-110"
  />

  <div
    class="absolute inset-0 bg-linear-to-t from-slate-950 via-slate-950/40 to-transparent opacity-90 md:opacity-70 md:group-hover:opacity-90 transition-opacity"
  >
  </div>

  <div class="absolute inset-0 p-8 flex flex-col justify-end">
    <div
      class="translate-y-0 md:translate-y-4 md:group-hover:translate-y-0 transition-transform duration-500"
    >
      <h4
        class="text-2xl font-semibold text-white mb-3 flex items-center justify-between"
      >
        {item.title}
        <span
          class="opacity-100 md:opacity-0 md:group-hover:opacity-100 transition-all duration-500 bg-white/20 p-2 rounded-full backdrop-blur-md scale-90 md:scale-100"
        >
          <ArrowUpRight size={20} class="text-white" />
        </span>
      </h4>

      <div
        class="h-auto opacity-100 mb-4 md:h-0 md:opacity-0 md:mb-0 md:group-hover:h-auto md:group-hover:opacity-100 md:group-hover:mb-4 transition-all duration-500 overflow-hidden"
      >
        <p
          class="text-white/80 text-sm font-light leading-relaxed line-clamp-3 md:line-clamp-none"
        >
          {item.description}
        </p>
      </div>

      <div
        class="h-1 bg-brand-primary rounded-full transition-all duration-500 w-full md:w-12 md:group-hover:w-full"
      >
      </div>
    </div>
  </div>
</div>
